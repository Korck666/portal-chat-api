name: Create Pull Request

on:
  push:
    branches:
      - production

jobs:
  create_pull_request:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@latest

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: production
          title: Automatically generated pull request
          body: This pull request was automatically generated by the GitHub Action.
          author: '${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>'

